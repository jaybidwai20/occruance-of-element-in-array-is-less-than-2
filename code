#include<bits/stdc++.h>
#include <iostream>

using namespace std;

int main()
{  
    // int counter=0;
   map<int,int>m;
   int n;
   cin>>n;
   int a[n];
   for(int i=0;i<n;i++)
   {
       cin>>a[i];
        m.insert({a[i],m[a[i]]++});
   }
   for(auto it=m.begin();it!=m.end();it++)
   {
     if((*it).second==1)
     {
         cout<<(*it).first<<endl;
     }
    
   }}
   
  
